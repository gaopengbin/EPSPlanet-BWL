import _CameraViewManager from './cameraViewManager.vue';
import { withInstall } from '../../utils';
export const CameraViewManager = withInstall(_CameraViewManager);
export default CameraViewManager;
